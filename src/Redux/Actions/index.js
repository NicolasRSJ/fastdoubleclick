import { createSlice, configureStore } from "@reduxjs/toolkit";

const configAddTime = {
    name: 'addtime',
    initialState: {
        time: 0
    },
    reducer: {
        addTime: (state) => {
            
        }
    }
}